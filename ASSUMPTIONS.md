# ASSUMPTIONS — STELE FPGA autonomous run

Every default taken, version pinned, and deviation from the spec. Started 2026-07-03.

## Toolchain (pinned)

- OSS CAD Suite **2026-07-03** linux-arm64 (`oss-cad-suite-linux-arm64-20260703.tgz`),
  from https://github.com/YosysHQ/oss-cad-suite-build/releases/tag/2026-07-03,
  installed at `~/eda/oss-cad-suite`. Provides:
  - Yosys 0.66+183 (b285b72e3)
  - nextpnr-ice40 0.10-82-g2b560ad0
  - Verilator 5.051 devel (v5.050-6)
  - Icarus Verilog 14.0 devel (s20260301-241)
  - icestorm (icepack/icetime/icepll)
  - Python 3.11 with cocotb 2.1.0.dev0+41564633
- numpy 2.4.6 installed into the suite's bundled Python via its pip.
- All build steps `source env.sh` (which sources the suite `environment`).
- cocotb is 2.x: tests use the 2.x API and `COCOTB_TEST_MODULES`/`COCOTB_TOPLEVEL`
  environment variables in the Makefile flow.

## Resolved defaults (runbook §5, pinning spec §15)

| Item | Value |
|---|---|
| HyperRAM behavioural model | Generic 3.3 V x8 HyperBus, ISSI IS66WVH-class; CSR-configurable fixed read latency (default 6 clocks); tCSM = 4 µs enforced by assertion in the model |
| Ternary packing | 2 bits/trit, 4 trits/byte (`00`=0, `01`=+1, `10`=−1, `11`=illegal→treated as 0); `PACKING` parameter hook left for 5-trit/byte |
| Vocabulary | `VOCAB=128` (7-bit tokens, single-beat token I/O) |
| Sim model dims | `D_MODEL=64, N_LAYERS=2, D_STATE=16, D_CONV=4, E=2` (D_INNER=128) |
| Clock | Single domain; PHY CK = clk/2 via `CLK_DIV` (CSR) |
| Multiplier | Synthesizable `mult_synth.v` default; `USE_DSP=0` |
| Full-gen token count | 8 tokens |
| RNG seed | **0xC0FFEE** (12648430) for all golden weight/trace generation |

## Golden reference (runbook §4 override)

- Weights are **seeded random** (seed above), quantised to ternary/int8 — not a
  trained checkpoint. Valid for bit-exact RTL verification; language quality is
  out of scope. `--from-checkpoint` hook not implemented in the first pass
  (best-effort per runbook; logged as skipped).
- Reference model is numpy-only (no torch, no mamba_ssm).

## Fixed-point spec (chosen; mirrored exactly in RTL and reference)

- Activations/state `h`: int8. Accumulators: int32.
- Requantisation: arithmetic right shift with round-to-nearest (add half-LSB
  before shift), then saturate to int8. Shift constants are part of the
  fixed-point spec (baked into RTL localparams and the reference identically):
  `S_IN=3, S_CONV=1, S_XP=2, S_DT=1, S_DB=4, S_SCAN=7, S_C=6, S_G=5, S_OUT=3`.
  Chosen from signal statistics of the seeded-random model so every stage
  keeps live int8 range (<1% saturation); an earlier worst-case-sized choice
  drove all activations to zero.
- A matrix: int8 in [-32,-2] (Q3.4, i.e. -2.0..-0.125) so Ā spans a useful
  decay range. dt_proj bias omitted; no RMSNorm (spec never budgets one);
  D-skip term omitted; residual add x+block(x) included; SiLU applied after
  conv (as in Mamba). DT_RANK=4.
- Δ, B, C: int8 after their projections; Ā ("A-bar") = PWL exp result in
  **Q0.7 unsigned** (0..127 ≈ 0..0.992).
- PWL tables: 8 segments per function (softplus, exp(x) for x≤0, SiLU),
  generated by `golden/gen_pwl.py` into `rtl/pwl_tables.vh` and used by both
  the numpy reference and the RTL — identical by construction.

## Deviations / notes

- `iverilog`/`icepack`/`icetime` verified by presence on PATH (they do not
  support `--version` cleanly under `pipefail`).
- No FPGA board attached; `iceprog` is never invoked (runbook §1). Deliverable
  is bitstream + green sims.
